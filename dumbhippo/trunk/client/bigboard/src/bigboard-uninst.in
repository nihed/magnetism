#!/bin/sh
srcdir=@SRCDIR@
builddir=$(dirname $0)
PYTHONPATH=${srcdir}/../canvas/build/.libs/:${srcdir}/../canvas/.libs:${builddir}/.libs:${srcdir}/src exec $BB_DEBUG_CMD @PYTHON@ @BB_PYDIR@/main.py "--stockdirs=@BB_STOCKDIR@" "$@"
