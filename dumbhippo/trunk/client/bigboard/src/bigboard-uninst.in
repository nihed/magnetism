#!/bin/sh
srcdir=@SRCDIR@
builddir=$(dirname $0)
PYTHONPATH=${srcdir}/../canvas/build/.libs/:${srcdir}/../canvas/.libs:${builddir}/.libs:${srcdir}/src exec $DEBUG @PYTHON@ ${srcdir}/src/main.py --stockdirs=$(pwd)/${srcdir}/src/stocks "$@"
